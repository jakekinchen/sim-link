# Apply these semantics only after the hardware gate is mechanically closed.
sandbox_mode = "danger-full-access"
approval_policy = "never"
developer_instructions = "Offline-autonomous permissions do not grant robot authority. Keep every physical-hardware gate closed."

[features]
multi_agent = false
