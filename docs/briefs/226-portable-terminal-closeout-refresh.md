# Slice Brief 226 - Portable Reconstruction Foundation and Fork Spine

**Date:** 2026-07-16
**Support task:** K2
**Current-repository policy boundary:** T20.43c-R2 verified terminal negative
**Fork target:** `freeze-2026-07-17-hackathon-fork`

## Objective

Turn the verified research boundary into a portable, non-authorizing
reconstruction kit that a new repository can bootstrap without tribal
knowledge. Preserve the current repository's exact evidence and authority
model through the freeze, while making the fork target intentionally smaller:
one pinned LeRobot interpreter, in-process rendering, state-first simulation,
automatic run receipts, and no historical task alphabet.

This brief owns W1 bootstrap/current-kit integration and the later W6 refresh.
W2 R0 regeneration, W3 portability rehearsal, and W5 gateway work remain
isolated candidate lanes until their signed handoffs are reviewed and
explicitly integrated.

## Ground-truth inputs

- T20.43b remains a verified terminal runtime failure at source boundary
  `4d8f951fc5e0f16d3f47dc44965e3221fe0c3778`; it is not a trained negative.
- Original T20.43c proved bit-exact checkpoint-0 continuation equivalence and
  rendered the actual trace schema before optimizer update 1. Its consumed
  marker and terminal artifact `d848a1a8...` remain an immutable inconclusive
  interruption at update 728 under a superseded scheduling instruction.
- T20.43c-R2 later completed the unchanged campaign: 10,000 updates, seven
  checkpoints, fourteen rollouts, no infrastructure failure, no Gate C pass,
  and no retry. Its immutable identities are marker `dfe3ff05...`, equivalence
  `fdc06297...`, run `82a06083...`, result `bf2c8b46...`, scorecard
  `6f848570...`, retention `e565e17a...`, and final `be11a258...`.
- The R2 release-phase counterexample is retained: final chunk-50 lifted
  0.037655304225193253 m and failed only `release_final_contact_clear`;
  maximum chunk-50 lift was 0.04567430422519325 m; final receding-10 had no
  grasp hold and lifted 0.000502 m.
- Commit `6d28e0c` preserves the W1 bootstrap foundation and signed portable
  asset pack. W1 must still prove the current repository's parent/child
  runtime and every retained trace schema before the fork deletes subprocess
  dispatch.
- W1 clean-export bootstrap receipt `42fcbe94...` now proves the pinned local
  dependency/runtime boundary, 29 focused tests, one 244-frame strict-v2
  expert episode, and all three retained trace-schema smokes in 45.841226 s;
  it explicitly grants no authority and creates no optimizer.
- Reviewed W2 candidate receipt `fd83af1d...` proves exact full-R0 recreation in
  4,425.745176 s with 119+9 successes, 129 episodes, 31,366 frames, 59,904
  windows, and no mismatches. Its seven-path overlay is integrated; a fresh
  combined W1+W2 export rehearsal remains required before K2 closes.
- Owner overnight direction `4589437` supersedes the old same-night freeze.
  The new target is `freeze-2026-07-17-hackathon-fork` after a full morning
  refresh.
- The ten subtractive fork decisions in
  `docs/autonomous-workflow/hackathon-fork-annex-2026-07-16.md` are the design
  source for the living fork spine. They describe the future fork, not current
  live authority.

## Allowed work

- Preserve and verify both T20.43c boundaries without restarting, mutating, or
  conflating them: original exact-equivalence/interruption evidence and the R2
  terminal marker/result/scorecard/retention/final receipt set.
- Maintain the signed portable asset pack and one-command W1 bootstrap:
  acquire exact external pins from local checkouts, create the current
  dual-runtime environment, verify the stack, run focused model-free tests,
  generate one constructive-expert episode with strict-v2 evidence, and smoke
  every retained trace schema.
- Reconcile the required living docs and reconstruction-kit spine to the ten
  fork decisions while keeping current-repository truth explicit.
- Pin the curated source manifest to a reviewed commit, export into a clean
  scratch directory, and run the offline/local W1 bootstrap rehearsal.
- Review W2, W3, or W5 handoffs only after their evidence exists; integration
  requires a separate explicit, path-scoped decision.
- Run model-free focused tests, formatting, link/manifest checks, clean-export
  checks, and a fresh contradiction review.

## Fork target contract

- At fork birth, collapse to the pinned LeRobot venv as the sole interpreter;
  render in-process and delete the current subprocess dispatch.
- Default iteration is camera-free state-based RL over joint state plus object
  pose, with 60-frame success-terminated episodes and a light parquet tier.
  Full audiovisual datasets belong only to demonstration/VLA work.
- ACT and state-based RL are primary until a demo works. SmolVLA and PI0.5 are
  day-three stretch tracks.
- Training may vary across accelerators. Evaluation is CPU/fp32 and produces
  bit-identical verdicts across supported Mac and Linux hosts.
- Each fork run automatically emits `RUN_RECEIPT.json` with commit, config
  hash, dataset identity, seed, wall clock, and metrics. That simplification
  does not replace current-repository authority or evidence before the freeze.
- The workcell XML is frozen; task additions are registry data. The gateway is
  the only future path to a real robot.
- Outputs are ignored from fork commit one, run names are human-readable, and
  the task alphabet is not carried forward.
- Frozen held-outs, replayable candidate artifacts, and separate evaluation
  ownership remain mandatory. Historical permits and reviewer records may be
  copied only as inert provenance.

## Authority withheld

No T20.43c/T20.43c-R2 retry, restart, third ACT attempt, marker rewrite, policy
result reinterpretation, recipe/gate/data change, Gate C execution, T20.45
activation, hardware/camera/serial access, physical motion, network package
acquisition, external compute, Brev, transfer, promotion, authority transfer,
bulk/private copying, or destructive operation is authorized by this support
brief. K3 camera authority, if any, is separately composed and is outside this
slice.

## Acceptance criteria

- Original T20.43c is remotely preserved as an inconclusive owner-directive
  interruption at update 728, and T20.43c-R2 is preserved as a verified
  terminal negative at 10,000 updates/seven checkpoints/fourteen rollouts,
  with all seven immutable identities, the release-phase counterexample, no
  infrastructure failure, and no retry claim.
- The living spine consistently distinguishes current dual-runtime proof from
  the fork's single-venv design and includes all ten annex decisions.
- The source selection and manifest include the signed portable assets and
  the compact T20.43c and T20.43c-R2 evidence required to reconstruct both
  boundaries while excluding expired live authority and the manual runner.
- A clean scratch export passes dependency-light tests and the W1 bootstrap
  from local external pins, including one constructive-expert episode and
  all-schema render smoke, and emits a signed non-authorizing rehearsal
  receipt with wall-clock timing.
- W2/W3/W5 outcomes are not claimed or integrated without reviewed evidence.
- Focused tests, doc/link/manifest checks, and the same-agent contradiction
  review pass; scoped commits are pushed and origin-confirmed before the fork
  spine is called integrated.
