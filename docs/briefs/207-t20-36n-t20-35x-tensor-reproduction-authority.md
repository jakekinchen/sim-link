# Brief 207 - T20.36n T20.35x Tensor Reproduction Authority

## Status

Active under the owner's overnight priority order. T20.36m verifies SmolVLA
negative, but the three-candidate amended-gate comparison is incomplete.
Retained T20.35x result `e79dacff...` and run `b5cc16ef...` contain five hashes
and aggregate errors only; neither retains a 50x6 decoded tensor.

## Objective

Reproduce and directly retain only the five already-evaluated T20.35x 50x6
physical action tensors, prove two repeats per registered seed are bit-identical
and match the five signed T20.35x hashes, then score frozen amended gate
`463477dc...`. This is inference-only evidence recovery, not training, retry,
or a new candidate.

## Requested Owner Authority

- Construct and load the exact retained T20.35x checkpoint once locally after
  fresh central composition, runtime preflight, one-use permit, review, remote
  preservation, and attempt-marker creation.
- Restore only the exact cached `pyarrow==25.0.0` distribution offline if the
  live source runtime still reports the known `24.0.0` drift; reject any other
  dependency difference and bind the complete live version map into preflight.
- Run exactly inference seeds 20260721-20260725 with two repeats each on the
  original canonical one-batch observation.
- Persist ten 50x6 physical action tensors and require their hashes to equal
  `ab47fa1d...`, `daeb4539...`, `34a75867...`, `8a89f1a9...`, and
  `6b952637...` before scoring.
- Apply frozen gate `463477dc...` without fitting or changing a threshold.

## Required Work

1. Mechanically bind T20.35x spec `96efc6d3...`, run `b5cc16ef...`, result
   `e79dacff...`, checkpoint `40c94f66...`, frozen gate `463477dc...`, and
   T20.36m's tensor-retention/hash-verification pattern.
2. Compose fresh inference-only central authority, dependency-complete runtime
   preflight, and one-use permit without model construction or tensor reads.
3. Commit, push, confirm origin, and record a same-agent pre-run reviewer
   decision before creating the attempt marker.
4. Reproduce ten tensors, fail closed on any hash/repeat/shape/finite-value or
   coordinate drift, and retain the tensor artifact directly bound by result.
5. Score every joint/timestep under the frozen reach/grasp thresholds and keep
   the uniform 0.05-rad pass reported separately.
6. A frozen amended-gate pass routes only to separately authorized one-episode
   Gate C bridge work; a failure completes the negative comparison.

## Prohibited Actions

No optimizer, training update, checkpoint mutation, new seed, third repeat,
threshold change, Gate C execution, policy acceptance/promotion, hardware,
camera, serial, network/download, any dependency change other than the exact
offline PyArrow 24-to-25 restore above, external compute, or Brev.

## Acceptance

All source identities and five prior hashes reconstruct exactly; both repeats
match per seed; the ten finite 50x6 tensors are directly bound by a tracked
result; frozen amended-gate scoring is complete; exact/focused/pointer tests and
same-agent adversarial review pass; and the pre-run and result boundaries are
scoped, pushed, and confirmed on origin.

## Verified Pre-Run Boundary

Implementations `69a2f46`, `1aab35f`, and `a77a829` are on origin. The exact cached
PyArrow correction restored `24.0.0` to the source-required `25.0.0` offline;
all six required dependency versions now match. Owner grant `a2b45382...`,
central decision `5b40131b...`, runtime preflight `8e99f9ad...`, and one-use
permit `2c6a1e78...` reconstruct exactly. The preflight byte-hashes both the
2.77 GB trainable checkpoint tree and exact pinned 14.47 GB frozen-base
snapshot tree `55544131...` without tensor deserialization. The runner mirrors
source RNG construction and validates every base-noise hash before decode. No
model construction/load/inference, optimizer action, or hardware access has
occurred. Reviewer 265 authorizes exactly one five-seed/two-repeat attempt only
after this corrected boundary is committed, pushed, and confirmed on origin.
