# Brief 205 - T20.36l Frozen Consequence Gate Amendment

## Objective

Materialize the owner's pre-authorized Gate B amendment as a frozen,
content-addressed, phase/joint threshold contract derived only from verified
T20.36k consequence evidence, then score the already-retained ACT and SmolVLA
evaluation tensors without loading a model or running inference. Keep the
historical uniform metric visible and route any consequence-gate pass only to
a separately authorized one-training-episode Gate C attempt.

## Required Work

1. Record the owner's pre-result amendment authorization and bind T20.36k
   result `a3b39178...`, Reviewer 261, and implementation `fbdb0aa`.
2. Freeze the exact reach/grasp phase thresholds applicable to the 50-step
   one-batch target from T20.36k. Threshold derivation may not read ACT or
   SmolVLA errors.
3. Retain objective ratio 0.10, deterministic fixed-repetition requirements,
   all-dimension/time reporting, and the strict uniform 0.05-rad result as a
   report-only diagnostic.
4. Reconstruct and hash the retained ACT and SmolVLA per-seed, per-repeat,
   per-timestep, per-joint errors directly from their immutable run artifacts.
5. Score both candidates under the frozen contract. No model construction,
   inference, optimizer, or new decode is allowed.
6. Route a passing candidate only to a separate Gate C authority request. A
   Gate B amendment pass is not policy acceptance, promotion, or Gate C proof.

## Acceptance

- Frozen thresholds exactly reconstruct from T20.36k and do not depend on any
  candidate value.
- Candidate tensors and hashes reconstruct from retained signed runs without a
  model call.
- ACT and SmolVLA receive the identical amended conjunction.
- The strict uniform metric, objective ratio, and all original negative labels
  remain visible; history is not rewritten.
- The artifact and scorer fail closed on missing tensors, hashes, repetitions,
  phase mapping, source drift, or non-finite values.
- No Gate C execution or policy selection occurs.

## Prohibited Actions

Threshold fitting to candidate errors, ACT/SmolVLA retry, model load,
inference, optimizer, new decode, silent relabeling of T20.36e/T20.36j, Gate C
execution, policy acceptance/promotion, hardware, external compute, or Brev.

## Verified Result

Owner decision `32d7e193...` and frozen amendment `463477dc...` bind the exact
T20.36k reach/grasp matrix before candidate scoring. Result `0f8ae393...`
conclusively fails ACT on shoulder lift t0, wrist roll t0, and gripper t49.
SmolVLA remains indeterminate and fails closed because its run retained five
matching hashes and aggregates but no 50x6 decoded tensor. No checkpoint weight
was read and no model, inference, optimizer, new decode, Gate C, selection,
hardware, external compute, or Brev action occurred. Implementation `e4c00bd`
is on origin; Reviewer 262 verifies this brief.
