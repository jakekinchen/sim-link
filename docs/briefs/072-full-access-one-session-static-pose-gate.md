# Slice Brief 072 - Full-Access One-Session Static-Pose Gate

**Date:** 2026-07-13

## Objective

Open exactly one finite T16.5c static-pose candidate session from the current
formally verified Full Access/no-prompt task. The transition is ineffective
until its scoped commit is confirmed on `origin/codex/pi05-autolearn-loop`.

## Contract

- The run window is `2026-07-13T08:21:09-05:00` through
  `2026-07-13T12:21:09-05:00`; no new major slice begins after `11:51:09`.
- The live gate is `08:30:00` through `09:15:00`, session limit one, sessions
  started zero, and scope `one_static_pose_bracket_candidate_session`.
- Exact runtime: thread `019f5ba2-1ed7-7f92-a89d-e1fe1f12720b`, turn
  `019f5ba2-24cf-7d03-8f54-7421a4276ccf`, profile `77f39177...`,
  `danger-full-access`, approval policy `never`, exact checkout.
- Before device open, recapture and reverify the same-thread profile, issue one
  fresh five-minute owner-presence lease, perform fresh metadata and Studio
  status discovery, prove exact follower identity, follower route none, no
  running hardware job, torque off, both stable cameras and exact modes, source
  calibration, zero holders across canonical and TTY aliases, the complete
  candidate contract, and a new immutable private destination.
- Execute only `q_before -> two frames per camera -> q_after -> no-torque close
  -> all-alias zero-holder verification`, then reclose on every outcome.

No handshake, register/configuration write, torque change, motion, retry,
reconnect, model execution, policy actuation, training, paid compute, or proof
label is granted by this transition.
