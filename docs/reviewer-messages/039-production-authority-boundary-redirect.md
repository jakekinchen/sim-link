# Reviewer Decision 039 - Production Authority Boundary Redirect

**Date:** 2026-07-10

## Decision

`REDIRECT THEN CONTINUE`

## Scope

This decision records the external review of exact unbranched commit `df9ed0a`,
not merely public `main`. It supersedes decision 038 where that decision closed
T16.4 or authorized T16.5 to begin.

## Accepted Evidence

- The training lock, bounded stages, proof-mode separation, immutable-artifact
  direction, evaluation-seed separation, and stop conditions remain accepted.
- T16.1 retains verified dependency inventory and source-pin evidence.
- T16.2 retains a verified qualification schema scaffold.
- T16.3 remains verified.
- T16.4 retains a truthful blocked real template plus a deterministic synthetic
  parallel-axis compiler scaffold and negative-path evidence.
- MPS inference/train/save/reload remains smoke proof only; current learned
  behavior remains a narrow reach improvement with no autonomous grasp or task
  success.

## Reopened Findings

- `100`: T16.1 accepts `split_runtime_unresolved`; collection, training,
  finalization, and inference are not yet bound to one executable LeRobot and
  preprocessing revision.
- `100`: T16.4 can reach `ready` only through `synthetic_test_only`, while its
  generic ready gate does not enforce the artifact's explicitly false training
  and physical authority.
- `100`: T16.2 qualification report status is caller-authored and is not
  independently recomputed from the specification tolerance.
- `100`: brief 031 does not yet prove no writes across teardown and failure
  cleanup, and a full writable motor bus is too broad for a read-only census.
- `75`: M20 simulation-only falsification should depend on M17-M18, while
  physical transfer eligibility remains gated by M19.
- `75`: repeated manual state sources permit contradictions such as a verified
  task with `Commit: pending`; machine-readable project state must become the
  authority.

## Required Order

1. Preserve the branch and dirty-worktree evidence.
2. T16.1b unified executable stack and processor conformance.
3. T16.4b strict production measured-inertial intake and authority lattice.
4. T16.2b mechanically computed qualification decisions.
5. Revised T16.5 full-lifecycle read-only census conformance.

No optimizer run is authorized before T16.1b passes. Training lock remains
closed throughout these corrections.
