# Reviewer Decision 098 - Open Full-Access One-Session Static-Pose Gate

**Date:** 2026-07-13

## Decision

`CONTINUE T16.5C; OPEN EXACTLY ONE READ-ONLY STATIC-POSE CANDIDATE SESSION AFTER REMOTE CONFIRMATION`

The exact current thread is independently verified at the required checkout
with `danger-full-access` and approval policy `never`. Reviewer 097 and the
canonical reconciliation are on origin. The owner states current physical
presence, and the finite run and live-gate windows are explicit.

The transition permits one fresh session only after its own commit is present
on `origin/codex/pi05-autolearn-loop`. Fresh profile, lease, discovery, Studio
status, exact follower/calibration/camera/mode, torque-off, all-alias zero-holder,
candidate-contract, and immutable-destination checks remain mandatory before
device open. The session must close with no torque change and all aliases free.

Evidence anchor `100`: any hardware open before remote confirmation or any
prerequisite failure is prohibited. This decision grants no observation
acceptance, reviewed policy input, model load, inference, shadow, replay,
actuation, qualification, training, or paid-compute authority.

Fresh same-agent adversarial review checked authority escalation, stale current-
task references, unsafe defaults, profile collapse, stale holders, gate expiry,
session double counting, cleanup side effects, and documentation contradictions.
The gate remains one-shot and ineffective before remote confirmation; stale
Studio and holder observations are explicitly prerequisites to refresh, not
current proof.

Forty-three focused static-pose execution/candidate/session/review tests pass in
each pinned robotics runtime. Both source verifiers pass, the workflow audit and
strict project-state assertions pass, `git diff --check` passes, and the broad
authority/twin regression passes 377 tests in 124.327 seconds. No hardware,
model, MuJoCo, motion, training, Brev, or paid compute path ran.
