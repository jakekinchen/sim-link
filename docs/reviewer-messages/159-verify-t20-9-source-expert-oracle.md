# Reviewer Decision 159 - Verify T20.9 Source-Expert Oracle

`CONTINUE`

Reviewed implementation boundary `970098b9a22ef9381acbe308b421cc3a710fef88`
and source-bound oracle identity
`cdb7d924416e47f0723743fa1f03d00ee1b9a4180d01c7f3e15a69e9057900eb`.

The seed-2 source episode regenerated byte-identically, and its exact 244
requested actions were consumed in order through the T20.7 policy rollout
adapter. Source, regenerated, and oracle action vectors, robot positions,
velocities, object positions, phases, and contact-geometry sequences have zero
measured divergence. No action was projected and no assistance was active.
Five 256 px keyframes visibly retain pregrasp, close, grasp hold, unsupported
lift, and clear retreat. The adapter reproduces the source trajectory and a
36.3853 mm lift.

The same trajectory is labeled strict success by T17.5b but rejected by T20.7
only at `release_final_contact_clear` (0 versus 1). The source success contract
does not contain that gate. The regenerated release-settle endpoint retains a
fixed-pad collision pair with no force-bearing pad aggregate and no non-pad
contact, while the final retreat is geometrically clear. T20.6 defines invalid
release in terms of retained fingertip contact, not any zero-force collision
pair. This is a source-versus-acceptance semantic mismatch, not an execution
divergence and not learned-policy success.

The deterministic write/check path passes, as do 75 relevant generation,
rollout, semantics, authority, acceptance, strict-grasp, and pointer tests.
Continue to a separately briefed release-semantics correction that preserves
force-bearing release clearance and final retreat geometry clearance, then
rerun the oracle before any optimizer work. Do not weaken strict contact,
accept a policy, or grant hardware, physical transfer, promotion, external
compute, or Brev authority.
