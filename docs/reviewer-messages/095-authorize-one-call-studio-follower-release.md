# Reviewer Decision 095 - Authorize One-Call Studio Follower Release

**Date:** 2026-07-12

## Decision

`CONTINUE - ONE EXACT FOLLOWER DISCONNECT CALL AFTER REMOTE CONFIRMATION`

The owner's fresh authority materially expands the previously consumed
disconnect scope. Reviewer 095 grants exactly one new call with the method,
endpoint, and body in Brief 069. It may release only the follower and may rely
only on the endpoint's inherent torque-disable behavior.

Same-agent adversarial review rejects retries, reconnect, leader mutation,
process killing, register/configuration writes, torque-enable, motion, policy
actuation, training, and any claim beyond follower-released/torque-off/zero-
holder evidence. The live candidate gate remains closed.
