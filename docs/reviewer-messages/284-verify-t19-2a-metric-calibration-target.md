# Reviewer Decision 284 - Verify T19.2a Metric Calibration Target

**Date:** 2026-07-16

## Decision

`ACCEPT PRINTABLE METRIC TARGET; CONTINUE T19.2B BOUNDED-MOTION HARNESS OFFLINE`

Brief 214 implementation `476de8fe48fb33ca506398e70b8b168eb06b65d4`
is on origin. Signed spec `6359a62f...` binds a deterministic vector US Letter
PDF `02af20f0...`: ten by seven exact 20.00 mm squares, nine by six inner
corners, 200.00 x 140.00 mm active area, black top-left orientation, and a
100.00 mm external verification bar. Printing must use 100 percent / Actual
Size with fit and shrink disabled and reject more than 0.20 mm scale error.

The PDF was regenerated byte-for-byte, inspected through pypdf and pdfinfo,
rendered at 144 dpi through Poppler, and visually reviewed. The page is clean:
the board is unclipped, square boundaries and origin labels are unambiguous,
the scale bar and print instructions are legible, and no text overlaps the
active target. Four PDF-runtime tests verify deterministic bytes, exact page
geometry, checked-in source binding, and re-signed geometry rejection. The
pinned robotics runtime safely skips those four optional-dependency tests while
still running the T19.2 readiness suite.

This establishes only `printable_metric_calibration_target_artifact_valid`.
It does not prove the target is printed at scale, physically placed, visible,
or measured. No live gate, camera/serial access, write, torque change, motion,
calibration, twin update, physical qualification, policy, external compute, or
Brev authority is granted. T19.2b must build and fixture-test the bounded
watchdog/deadman/stop/exact-return motion harness next.
