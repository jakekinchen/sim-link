# Reviewer Decision 096 - Accept Studio Follower Release

**Date:** 2026-07-12

## Decision

`CONTINUE - ACCEPT ONE-CALL FOLLOWER RELEASE CORRECTION`

Reviewer 095's exact operation completed with call count one, HTTP 200,
follower disconnected, torque false, leader unchanged, and zero holders across
both aliases. The corrective permit is consumed and cannot be reused.

This removes the concrete Reviewer 094 blocker but does not open a live gate,
issue a lease/candidate, grant a static-pose label, run a model, or authorize
motion/training. A new remotely preserved finite gate and fresh preflight remain
mandatory.
