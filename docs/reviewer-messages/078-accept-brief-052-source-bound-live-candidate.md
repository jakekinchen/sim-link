# Reviewer Decision 078 - Accept Brief 052 Source-Bound Live Candidate

**Date:** 2026-07-11

## Decision

`CONTINUE T16.5C; ACCEPT SOURCE-BOUND CANDIDATE FOUNDATION; LIVE GATE CLOSED`

Implementation `79eee894fe3aa60380664bb53821d7ad1482e174` and verification
follow-up `2c0b903a72b04f2d83edd5855705992e09712bc1` are present on
`origin/codex/pi05-autolearn-loop`.

The candidate contract binds the exact canonical project-state snapshot and
digest, a reviewed open one-session gate with zero sessions started, active
presence lease, accepted discovery/follower USB/all-alias identity, stable
camera identities resolved to fresh numeric indexes, exact pinned manifest,
calibration, and static-bracket sources, finite duration, operation counts, and
no-write lifecycle. The production runner can emit only candidate evidence and
cannot grant a physical proof label.

Production live and deterministic fixture builders, runners, and verifiers are
fixed separate classes. The shared runtime emits an unclassified capture, and
the caller must supply an exact approved transport/camera evidence-mode pair.
Fixture evidence records `hardware_opened=false`; re-signing the result with a
live class fails. The accepted private-discovery resolver proves stable camera
binding through numeric-index churn without opening or enumerating hardware.

Seventy-seven focused tests pass in each pinned runtime. Private-discovery and
source-chain verifiers, compilation, safety/privacy/JSON/diff checks pass, and
276 broad authority/twin tests pass in 72.567 seconds. Adversarial coverage
rejects gate, lease, source, follower, alias, holder, camera, lifecycle, timing,
count, pose, evidence-class, and authority drift, including attempted writes,
torque changes, motion, and cleanup failure suppression.

Grant only `static_pose_live_candidate_contract_valid` and
`source_bound_static_pose_live_candidate_runtime_conformant`. Do not grant a
live candidate result, `static_pose_bracketed_observation`, policy-input
validity, shadow, motion, physical qualification/transfer, promotion, or
training authority.

Before any live-gate transition, verify in a separate offline slice the exact
pinned Feetech and FFmpeg factories, immutable private success/failure evidence,
and a machine-checked `hardware_supervised_on_request` execution profile. The
actual runtime must use `approval_policy=on-request`; a fresh finite run window,
separate reviewed remote gate transition, fresh presence lease, and fresh
discovery/all-alias zero-holder snapshots are also mandatory. Until all are
true, the live gate and training lock remain closed.
