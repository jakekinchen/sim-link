# Reviewer Decision 281 - Open T19.1 Owner-Present Read-Only Census Gate

**Date:** 2026-07-16

## Decision

`CONTINUE T19.1; OPEN EXACTLY ONE FINITE READ-ONLY SIX-SERVO CENSUS SESSION AFTER REMOTE CONFIRMATION`

Brief 212 implementation `288e938b4ce9b0458af1ffdd45a63cb5af31e46f`
is confirmed on `origin/codex/pi05-autolearn-loop`. The implementation adds a
T16.2b-A central-composer decision and one-use permit that derive authority
from the exact owner window, current canonical task state, active same-thread
Full Access/no-prompt evidence, clean scoped sources, exact branch, and equal
local/upstream/remote boundaries. The gate transition in this reviewer decision
and canonical state remains ineffective until its own commit is also confirmed
on that remote.

The permitted live sequence is serial metadata enumeration, exact follower
resolution, zero-holder proof across callout and TTY aliases, one
`handshake=False` raw-bus connection, exactly 54 allowlisted scalar reads with
no retry, one `disconnect(disable_torque=False)`, and a second zero-holder
proof. The runner does not enumerate or open cameras. It cannot construct
`SOFollower`, a leader, or a robot aggregate. Any transient read is terminal.
Any nonzero torque-enable value rejects the candidate.

## Adversarial review

Fresh same-agent review checked authority escalation, stale references,
re-signed request/decision/permit drift, wrong or expired owner windows,
cross-thread/restricted runtime evidence, unconfirmed or dirty remote
boundaries, output path escape, permit reuse, serial ambiguity, holder presence,
source drift, retries beyond the exact read plan, write-like methods, torque
changes, motion, camera factories, malformed/non-finite values, teardown
failure, evidence tampering, and raw hardware-serial leakage into the tracked
manifest. The gate remains fail-closed on every checked condition.

Both pinned robotics runtimes passed the same 105 focused authority, census,
live-observation, runtime-profile, live-session, and T19.1 tests. Both static
live-execution source verifiers and candidate-source verifiers passed. Python
compilation, strict project-state JSON, static prohibited-call inspection, and
`git diff --check` passed. No device was enumerated or opened during
implementation or review.

## Authority

After this transition is remote, exactly one five-minute-or-shorter permit may
be composed before the earlier run hard-closeout. A successful reviewed result
may establish only `t19_1_live_readonly_hardware_snapshot_observed`. No camera,
write, torque change, motion, calibration, policy actuation, physical-twin
qualification, physical transfer, promotion, external compute, or Brev
authority is granted.
