# Reviewer Decision 319 - Verify K2 Portable Reconstruction Foundation

**Date:** 2026-07-17

## Decision

`VERIFY_K2_PORTABLE_RECONSTRUCTION_FOUNDATION`

Brief 226 satisfies its reconstruction acceptance criteria. The capsule now
preserves the terminal research truth, the simplified fork design, a verified
clean bootstrap, and an exact full-R0 regeneration path without transplanting
live authority or bulk outputs.

## Verified evidence

- origin contains source boundary
  `992ed2f5e40d11e3979cc206eebdfac20a004073`;
- manifest `d5396251...` selects 417 files / 77,602,177 bytes, hashes five
  explicit omissions, rejects forbidden data classes, and keeps authority,
  physical proof, and learned-policy success false;
- source-pin validation export `ae7cfd7c...` verifies 438 files / 77,916,341
  bytes, and final wrapper export `97daeb06...` verifies 438 files / 77,918,520
  bytes after all closeout edits;
- final-pin W1 receipt `7d9fa11a...` proves exact local dependency pins,
  Python 3.12.12, 29 focused tests, a 244-frame strict-v2 expert success, and
  all three retained trace-schema renders;
- combined W2 receipt `86739578...` exactly matches 119+9 successes, 129
  episodes, 31,366 frames, 59,904 windows, mixture `37b30d34...`, statistics
  `02ba0e70...`, and every lower-level frozen identity with `mismatches={}`;
- W2's independent verifier exits 0; both tracked W1/W2 receipts match their
  scratch source bytes exactly;
- 13 kit tests, seven R0 contract tests, manifest/asset verification, Python
  compilation, JSON parsing, documentation-link checks, and whitespace checks
  pass.

## Adversarial review

The scoped diff preserves all seven immutable T20.43c-R2 identities and does
not relabel the original T20.43c interruption. Current-repo dual-runtime proof
is distinct from the fork's single-venv/in-process design. Legacy full
audiovisual R0 remains a compatibility/VLA tier; state-first RL remains light
parquet with 60-frame success termination. The frozen held-out set,
replayable-artifact rule, and separate evaluation ownership remain intact.

No non-finite value, path traversal, symlink alias, silent output inclusion,
stale-permit activation, authority escalation, W3 completion claim, W5 gRPC or
policy-quality claim, learned Gate C claim, or destructive cleanup appears.
The optional F1 source-repo experiment does not promote PI0.5 into a primary
fork track. The new F0 route is a separate owner-directed experiment, not a
retroactive retry grant from R2.

## Authority disposition

K2 is verified and closed. W3 and W5 remain unintegrated. The next eligible
task is a fresh F0 diagnostic brief. Corrective training, F1/Brev execution,
hardware, physical motion, transfer, promotion, and the freeze tag remain
closed until their own current reviewed boundaries are satisfied.
