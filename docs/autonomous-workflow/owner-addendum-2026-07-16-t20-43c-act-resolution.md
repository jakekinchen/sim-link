# Owner Addendum - T20.43c ACT Resolution

**Recorded:** 2026-07-16

## Owner statement

> proceed, i authorize anything further actions that resolve this for us.

## Bounded interpretation

This direction authorizes the repository to resolve the still-unanswered
ACT-on-129-episodes question after T20.43b consumed its marker at zero optimizer
updates on a mirror-schema integration failure. It supersedes the prior
no-continuation/no-second-replacement closeout only through a fresh, separately
reviewed and centrally composed recovery boundary.

The preferred route is one exact zero-update continuation if—and only if—a
model-free implementation and post-marker equivalence check prove all of the
following:

- the original attempt marker, terminal receipt, checkpoint-0 tree, and
  chunk-50 trace remain byte-immutable;
- the actual T20.43b trace schema renders through the immutable-safe v2
  entrypoint before any new model action;
- the saved checkpoint-0 tensors equal a freshly seeded ACT initialization;
- the replacement optimizer has zero state entries and the deterministic
  episode sampler has consumed zero training batches; and
- the continuation result binds the new permit and marker in addition to the
  original attempt identity.

If any equivalence condition fails, no continuation model action may occur.
The route must stop and design one clean separately named replacement with new
paths; it may not delete, move, rewrite, or alias the consumed evidence.

## Authorized recovery outcome

After implementation, tests, same-agent review, origin preservation, fresh
central composition, runtime preflight, one-use continuation permit, and a
second pre-run review agree, exactly one local-MPS recovery campaign may:

- load the immutable checkpoint-0 ACT tensors;
- create a fresh empty AdamW optimizer with the unchanged recipe;
- complete the missing checkpoint-0 receding-10 evaluation;
- execute updates 1 through 10,000 exactly once;
- save checkpoints 500/1,000/2,500/5,000/7,500/10,000; and
- run both frozen execution semantics at every trained checkpoint, preserving
  either the first strict-v2 Gate C pass or the exact terminal negative/failure.

The existing checkpoint-0 chunk-50 rollout counts as the first of the fixed 14
evaluations. No correction objective, threshold, dataset, recipe, schedule,
seed, action semantics, or Gate C change is authorized.

## Deliberately unused broader authority

The owner statement is broad, but this route does not need and therefore does
not exercise hardware, camera, serial, physical motion, network acquisition,
package installation, paid/external compute, Brev, transfer, promotion, branch
destruction, history rewrite, or unrelated data mutation. Those remain closed.
